import React from 'react'

class PictureSlide extends React.Component {
    render() {
        return (
            <div>
            </div>
        );
    }
}

PictureSlide.propTypes = {
    url: React.PropTypes.string,
    width: React.PropTypes.number,
    height: React.PropTypes.number,
}

export default PictureSlide;
